noinst_LIBRARIES = liblunch.a
liblunch_a_SOURCES = lunch.c lunch.h

bin_PROGRAMS = lunch
lunch_SOURCES = main.c lunch.h
lunch_CFLAGS = -Wall -g
lunch_LDFLAGS =
lunch_LDADD = liblunch.a

dist_bin_SCRIPTS = menu.lst

CLEANFILES = *~
